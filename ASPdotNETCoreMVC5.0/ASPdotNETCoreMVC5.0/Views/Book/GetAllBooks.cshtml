@{
    ViewData["Title"] = "All books";
}

@model IEnumerable<BookModel>
@*@{        // Pass lots of data using Anchor tag
        var parameters = new Dictionary<string, string>()
        {
            {"name","Birol" },
            {"company","WebBook" },
            {"appName","Book App" }
        };

        // add in Html
        asp-all-route-data="@parameters"
        
        // Pass fragmentbusing Anchor tag
        asp-fragment="similarBooks" 

        // Pass complete url using Anchor tag
        asp-protocol="http" 
        asp-host="webBook.com"
    }*@


<div class="container">
    <h4 class="display-5">All Books</h4>
    <div class="row">

        @foreach (var book in Model)
        {
        <div class="col-md-4">
            <partial name="_bookThumbnail" model="@book"/>
        </div>

        }

    </div>
</div>